@using MassMailWeb.Helpers

@{
	int rowCounter = 0;
}

<head>
	<title>Histórico</title>
	<script>
		// TODO: Fix infinite update of rows
	</script>
	<link rel="stylesheet" href="~/Styles/History.css" />
</head>

<div class="table bg-white rounded border" style="overflow-y: scroll; height: 70%; width: 80%;">
	<table id="history-table">
		<tr class="text-center">
			<td>From</td>
			<td>To</td>
			<td>Subject</td>
			<td>Body</td>
			<td>When</td>
		</tr>
		@*@if (DbHelper.emailCounter != rowCounter)
		{*@
		@foreach (var email in Model)
		{
			<tr class="text-center">
				<td>@email.From</td>
				<td>@email.To</td>
				<td>@email.Subject</td>
				<td>@email.Body</td>
				<td>@email.When</td>
			</tr>
		}
		@*}*@
	</table>
</div>

<div id="delete-history">
	<form>
		<button  class="btn btn-primary btn-block" type="submit">Apagar histórico</button>
	</form>
</div>